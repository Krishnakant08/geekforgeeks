	
	int search(string text, string pat)
	{
	    // Your code goes here
	    int n=text.size();
	    int m=pat.size();
	    
	    for(int i=0;i<n;i++){
	        string temp=text.substr(i,m);
	        if(temp==pat){
	            return 1;
	        }
	    }
	    return 0;
	}
